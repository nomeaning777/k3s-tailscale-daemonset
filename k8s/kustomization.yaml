apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kube-system

resources:
  - configmap.yaml
  - rbac.yaml
  - daemonset.yaml

images:
  - name: ghcr.io/nomeaning777/k3s-tailscale-daemonset
    newTag: latest